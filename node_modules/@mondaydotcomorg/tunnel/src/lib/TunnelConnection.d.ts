import { TunnelCluster, TunnelRequestInfo } from './TunnelCluster';
import { Subject } from 'rxjs';
import { Logger } from 'pino';
import { TunnelInfo, TunnelOptions } from './types';
export declare class TunnelConnection {
    readonly opts: TunnelOptions;
    tunnelCluster?: TunnelCluster;
    info?: TunnelInfo;
    $open: Subject<string>;
    $error: Subject<Error>;
    private closeSubject;
    $close: import("rxjs").Observable<boolean>;
    $request: Subject<TunnelRequestInfo>;
    logger?: Logger;
    get url(): string | undefined;
    constructor(opts: TunnelOptions);
    open(): Promise<TunnelInfo>;
    close(): void;
    private getInfo;
    private init;
    private establish;
}
